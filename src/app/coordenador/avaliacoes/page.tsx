'use client'
import { useState, useEffect, useCallback } from 'react'
interface Disciplina { id: string; nome: string; cor_hex: string }
interface Professor { id: string; nome: string; email: string }
interface AvaliacaoDisciplina { id: string; status: string; ordem: number; disciplinas: Disciplina; usuarios: Professor }
interface Avaliacao { id: string; titulo: string; bimestre: number; ano: number; serie: string; turmas: string[]; data_aplicacao: string; status: string; instrucoes: string; avaliacao_disciplinas: AvaliacaoDisciplina[] }
const STATUS_CORES: Record<string,string> = { pendente:'bg-gray-100 text-gray-600', em_elaboracao:'bg-blue-100 text-blue-700', submetido:'bg-yellow-100 text-yellow-700', aprovado:'bg-green-100 text-green-700' }
export default function AvaliacoesPage() {
  const [avaliacoes, setAvaliacoes] = useState<Avaliacao[]>([])
  const [disciplinas, setDisciplinas] = useState<Disciplina[]>([])
  const [professores, setProfessores] = useState<Professor[]>([])
  const [aba, setAba] = useState<'listar'|'criar'>('listar')
  const [aberta, setAberta] = useState<string|null>(null)
  const [salvando, setSalvando] = useState(false)
  const [mensagem, setMensagem] = useState<{tipo:'sucesso'|'erro';texto:string}|null>(null)
  const [form, setForm] = useState({ titulo:'', bimestre:1, ano:new Date().getFullYear(), serie:'', turmas:'', data_aplicacao:'', instrucoes:'' })
  const [dp, setDp] = useState<{disciplina_id:string;professor_id:string}[]>([])
  const carregar = useCallback(async () => {
    const [avRes, dRes, pRes] = await Promise.all([fetch('/api/coordenador/avaliacoes').then(r=>r.json()), fetch('/api/disciplinas').then(r=>r.json()), fetch('/api/coordenador/professores').then(r=>r.json())])
    setAvaliacoes(Array.isArray(avRes)?avRes:[]); setDisciplinas(Array.isArray(dRes)?dRes:[]); setProfessores(Array.isArray(pRes)?pRes:[])
  }, [])
  useEffect(()=>{carregar()},[carregar])
  const addDp = () => setDp(prev=>[...prev,{disciplina_id:'',professor_id:''}])
  const remDp = (i:number) => setDp(prev=>prev.filter((_,idx)=>idx!==i))
  const updDp = (i:number, campo:string, valor:string) => setDp(prev=>prev.map((d,idx)=>idx===i?{...d,[campo]:valor}:d))
  const criar = async () => {
    if(!form.titulo||!form.serie||dp.length===0){setMensagem({tipo:'erro',texto:'Preencha titulo, serie e adicione ao menos uma disciplina.'});return}
    if(dp.some(d=>!d.disciplina_id||!d.professor_id)){setMensagem({tipo:'erro',texto:'Selecione disciplina e professor para cada linha.'});return}
    setSalvando(true);setMensagem(null)
    const res = await fetch('/api/coordenador/avaliacoes',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,turmas:form.turmas.split(',').map(t=>t.trim()).filter(Boolean),disciplinas_professores:dp})})
    const data = await res.json();setSalvando(false)
    if(res.ok){setMensagem({tipo:'sucesso',texto:'Avaliacao criada!'});setForm({titulo:'',bimestre:1,ano:new Date().getFullYear(),serie:'',turmas:'',data_aplicacao:'',instrucoes:''});setDp([]);carregar();setTimeout(()=>{setAba('listar');setMensagem(null)},2000)}
    else{setMensagem({tipo:'erro',texto:data.error??'Erro ao criar.'})}
  }
  const prog = (av:Avaliacao) => { const total=av.avaliacao_disciplinas?.length??0; const prontas=av.avaliacao_disciplinas?.filter(d=>d.status==='submetido'||d.status==='aprovado').length??0; return {total,prontas} }
  return (
    <div className='min-h-screen bg-gray-50'>
      <header className='bg-white border-b px-6 py-4 flex items-center justify-between'>
        <div className='flex items-center gap-3'><div className='w-8 h-8 bg-purple-600 rounded-lg flex items-center justify-center'><span className='text-white text-sm font-bold'>A</span></div><span className='font-semibold text-gray-900'>AvaliaEscola</span><span className='text-gray-400'>›</span><span className='text-gray-600 text-sm'>Avaliacoes Multidisciplinares</span></div>
        <a href='/coordenador/dashboard' className='text-sm text-indigo-600 hover:underline'>← Painel</a>
      </header>
      <main className='max-w-5xl mx-auto px-6 py-8'>
        <div className='flex gap-2 mb-6'>
          <button onClick={()=>setAba('listar')} className={'px-4 py-2 rounded-lg text-sm font-medium transition '+(aba==='listar'?'bg-purple-600 text-white':'bg-white border text-gray-600')}>Avaliacoes</button>
          <button onClick={()=>setAba('criar')} className={'px-4 py-2 rounded-lg text-sm font-medium transition '+(aba==='criar'?'bg-purple-600 text-white':'bg-white border text-gray-600')}>+ Nova Avaliacao</button>
        </div>
        {mensagem&&<div className={'mb-4 p-4 rounded-xl text-sm font-medium '+(mensagem.tipo==='sucesso'?'bg-green-50 text-green-700 border border-green-200':'bg-red-50 text-red-700 border border-red-200')}>{mensagem.texto}</div>}
        {aba==='listar'&&(<div className='space-y-4'>{avaliacoes.length===0?(<div className='bg-white rounded-2xl border p-12 text-center'><p className='text-4xl mb-3'>📝</p><p className='font-semibold text-gray-700'>Nenhuma avaliacao criada ainda</p></div>):avaliacoes.map(av=>{const {total,prontas}=prog(av);return(<div key={av.id} className='bg-white rounded-2xl border overflow-hidden'><div className='p-5 cursor-pointer hover:bg-gray-50' onClick={()=>setAberta(aberta===av.id?null:av.id)}><div className='flex items-start justify-between'><div><div className='flex items-center gap-2 mb-1'><span className='text-xs px-2 py-0.5 rounded-full bg-purple-50 text-purple-700 font-medium'>{av.bimestre}BIM {av.ano}</span><span className='text-xs px-2 py-0.5 rounded-full bg-gray-100 text-gray-600'>{av.serie}</span>{av.turmas?.map(t=>(<span key={t} className='text-xs px-2 py-0.5 rounded-full bg-blue-50 text-blue-600'>Turma {t}</span>))}</div><h2 className='font-semibold text-gray-900 text-lg'>{av.titulo}</h2></div><div className='flex items-center gap-4'><div className='text-center'><p className='text-xs text-gray-400 mb-1'>Progresso</p><p className='font-semibold text-gray-700'>{prontas}/{total}</p></div><span className='text-gray-400'>{aberta===av.id?'▲':'▼'}</span></div></div><div className='mt-3'><div className='h-2 bg-gray-100 rounded-full overflow-hidden'><div className='h-full bg-purple-500 rounded-full' style={{width:total>0?((prontas/total)*100)+'%':'0%'}}/></div></div></div>{aberta===av.id&&(<div className='border-t'><div className='p-4 bg-gray-50 space-y-2'>{av.avaliacao_disciplinas?.sort((a,b)=>a.ordem-b.ordem).map(ad=>(<div key={ad.id} className='bg-white rounded-xl border p-4 flex items-center justify-between'><div className='flex items-center gap-3'><div className='w-3 h-3 rounded-full' style={{backgroundColor:ad.disciplinas?.cor_hex??'#888'}}/><div><p className='font-medium text-gray-800'>{ad.disciplinas?.nome}</p><p className='text-xs text-gray-400'>{ad.usuarios?.nome}</p></div></div><span className={'text-xs px-3 py-1 rounded-full font-medium '+STATUS_CORES[ad.status]}>{ad.status==='pendente'?'Pendente':ad.status==='em_elaboracao'?'Em elaboracao':ad.status==='submetido'?'Submetido':'Aprovado'}</span></div>))}</div></div>)}</div>)})}</div>)}
        {aba==='criar'&&(<div className='bg-white rounded-2xl border p-6 space-y-5'><h2 className='font-semibold text-gray-900 text-lg'>Nova Avaliacao Multidisciplinar</h2><div className='grid grid-cols-2 gap-4'><div className='col-span-2'><label className='block text-sm font-medium text-gray-700 mb-1'>Titulo</label><input value={form.titulo} onChange={e=>setForm({...form,titulo:e.target.value})} placeholder='Ex: Avaliacao Multidisciplinar 1BIM 2026' className='w-full border rounded-xl px-4 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-purple-400'/></div><div><label className='block text-sm font-medium text-gray-700 mb-1'>Bimestre</label><select value={form.bimestre} onChange={e=>setForm({...form,bimestre:parseInt(e.target.value)})} className='w-full border rounded-xl px-4 py-2 text-sm'>{[1,2,3,4].map(b=><option key={b} value={b}>{b}BIM</option>)}</select></div><div><label className='block text-sm font-medium text-gray-700 mb-1'>Ano</label><input type='number' value={form.ano} onChange={e=>setForm({...form,ano:parseInt(e.target.value)})} className='w-full border rounded-xl px-4 py-2 text-sm'/></div><div><label className='block text-sm font-medium text-gray-700 mb-1'>Serie</label><input value={form.serie} onChange={e=>setForm({...form,serie:e.target.value})} placeholder='Ex: 1a Serie EM' className='w-full border rounded-xl px-4 py-2 text-sm'/></div><div><label className='block text-sm font-medium text-gray-700 mb-1'>Turmas (virgula)</label><input value={form.turmas} onChange={e=>setForm({...form,turmas:e.target.value})} placeholder='A, B, C, D' className='w-full border rounded-xl px-4 py-2 text-sm'/></div><div><label className='block text-sm font-medium text-gray-700 mb-1'>Data Aplicacao</label><input type='date' value={form.data_aplicacao} onChange={e=>setForm({...form,data_aplicacao:e.target.value})} className='w-full border rounded-xl px-4 py-2 text-sm'/></div><div className='col-span-2'><label className='block text-sm font-medium text-gray-700 mb-1'>Instrucoes</label><textarea value={form.instrucoes} onChange={e=>setForm({...form,instrucoes:e.target.value})} rows={2} className='w-full border rounded-xl px-4 py-2 text-sm'/></div></div><div><div className='flex items-center justify-between mb-3'><label className='text-sm font-medium text-gray-700'>Disciplinas e Professores</label><button onClick={addDp} className='px-3 py-1.5 bg-purple-100 text-purple-700 text-sm rounded-lg'>+ Adicionar</button></div>{dp.length===0?(<div className='border-2 border-dashed border-gray-200 rounded-xl p-6 text-center text-gray-400 text-sm'>Clique em Adicionar para vincular disciplinas e professores</div>):(<div className='space-y-2'>{dp.map((d,i)=>(<div key={i} className='flex gap-3 items-center'><span className='text-sm text-gray-500 w-4'>{i+1}.</span><select value={d.disciplina_id} onChange={e=>updDp(i,'disciplina_id',e.target.value)} className='flex-1 border rounded-xl px-3 py-2 text-sm'><option value=''>Disciplina</option>{disciplinas.map(disc=><option key={disc.id} value={disc.id}>{disc.nome}</option>)}</select><select value={d.professor_id} onChange={e=>updDp(i,'professor_id',e.target.value)} className='flex-1 border rounded-xl px-3 py-2 text-sm'><option value=''>Professor</option>{professores.map(p=><option key={p.id} value={p.id}>{p.nome}</option>)}</select><button onClick={()=>remDp(i)} className='text-red-400 hover:text-red-600 font-bold px-2'>x</button></div>))}</div>)}</div><button onClick={criar} disabled={salvando} className='w-full py-3 rounded-xl bg-purple-600 text-white font-semibold hover:bg-purple-700 disabled:opacity-50'>{salvando?'Criando...':'Criar Avaliacao'}</button></div>)}
      </main>
    </div>
  )
}